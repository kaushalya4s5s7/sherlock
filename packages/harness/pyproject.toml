[project]
name = "harness"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["contracts", "intake", "graph", "belief", "pattern", "policy", "reply", "stop", "language", "answer"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["harness"]
